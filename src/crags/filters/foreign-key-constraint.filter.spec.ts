import { ForeignKeyConstraintFilter } from './foreign-key-constraint.filter';

describe('ForeignKeyConstraintFilter', () => {
  it('should be defined', () => {
    expect(new ForeignKeyConstraintFilter()).toBeDefined();
  });
});
